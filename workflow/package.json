{
  "name": "@platforma-open/milaboratories.antibody-sequence-liabilities.workflow",
  "version": "1.0.0",
  "type": "module",
  "description": "Block Workflow",
  "scripts": {
    "pretty": "prettier --write \"./**/*.{js,jsx,mjs,cjs,ts,tsx,json,vue}\"",
    "build": "turbo run build",
    "build:dev": "PL_PKG_DEV=local turbo run build",
    "test": "PL_PKG_DEV=local turbo run test --concurrency 1",
    "mark-stable": "turbo run mark-stable",
    "watch": "turbo watch build",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "dependencies": {
    "@platforma-open/milaboratories.antibody-sequence-liabilities.liabilities-calc-script": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "vitest": "catalog:"
  }
}
